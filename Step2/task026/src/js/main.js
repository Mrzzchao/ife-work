var $ = function(str) {
    return document.querySelector(str);
}

/**
 * 命令传输格式
 * @param  {[type]} id  飞船编号
 * @param  {[type]} cmd 命令
 * @return {[type]}     [description]
 */
var Message = function(id, cmd) {
    this.id = id;
    this.cmd = cmd;
}

var btnHandler = function(commader) {
    var id = null;
    var cmd = null;

    var
}
